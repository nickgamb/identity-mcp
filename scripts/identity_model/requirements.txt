# Requirements for Identity Model Training
# 
# Install with: pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
# Or for GPU: pip install -r requirements.txt (uses default PyPI)

numpy>=1.24.0
torch>=2.0.0
torchvision>=0.15.0
sentence-transformers>=2.2.0
scikit-learn>=1.3.0
tqdm>=4.65.0

